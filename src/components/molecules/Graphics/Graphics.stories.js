import React from 'react'
import { Graphics } from './index'

export default {
  title: 'Molecules/Graphics',
  componente: Graphics
}

const Template = (args) => <Graphics {...args} />

export const FristGraphics = Template.bind({})
