import React from 'react'
import { TourModal } from './'

export default {
  title: 'Atoms/TourModal',
  component: TourModal
}

export const TourModalDefault = (top = '26', left = '42') => {
  return <TourModal />
}
